extends:
  - recommended

rules:
  # Существующие task-маршруты различаются статическими сегментами после
  # параметра и однозначно разрешаются chi-router. Переименование нарушило бы
  # публичный REST-контракт.
  no-ambiguous-paths: off
