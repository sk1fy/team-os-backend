apiVersion: 1

providers:
  - name: TeamOS
    orgId: 1
    folder: TeamOS
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
